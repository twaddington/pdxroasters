/**
 * PDX Roaster Utils
 *
 * @dependencies:
 * /static/js/ender/*
 * /static/js/pdx.js
 *
 */
(function () {

// Utility space
window.pdx.utils = {
    scrollTo: function ( $elem ) {
        var destination = $elem.offset().top;
        
        console.log( "need to figure this out" );
    }
};

})();