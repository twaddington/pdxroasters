/**
 * PDX Roaster Utils
 *
 */
(function () {

// Utility space
window.pdx.utils = {
    scrollTo: function ( $elem ) {
        var destination = $elem.offset().top;
        
        console.log( "need to figure this out" );
    }
};

})();